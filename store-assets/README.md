# Chrome Web Store assets

These files are prepared for the Chrome Web Store listing and are separate from the extension runtime.

- `promo-1400x560.png` — promotional tile / marquee image.
- `screenshot-01-ready.png` — supported-page ready state.
- `screenshot-02-running.png` — active session state.
- `screenshot-03-complete.png` — completed session state.

The runtime icon sources are in `../assets/`: use the PNG sizes in `../assets/icons/` for the extension listing and toolbar icon fields.

Suggested listing copy:

> Remove published LinkedIn company posts with clear timing controls, a session limit, and live progress. Runs only on the LinkedIn Page Posts admin page and uses the page UI directly.

Before publishing, confirm the listing text, screenshots, privacy disclosures, and Chrome Web Store policy requirements against the current extension behavior.
